/* import { useState, useEffect } from "react"
import axios from 'axios'

const API_ENDPONIT = 'https://cataas.com/'
const catFact = 'https://catfact.ninja/'

export default setGatito() => {

    const [gatitoUrl, setImage] = useState()
    const useEffect(() =>{
            axios.get(API_ENDPONIT`cat/says/${catFact}fontSize=50&fontColor=red`)
                .then(response => {
                    setImage(response.data)
                })
        },[]
    )   

    return 

} */